import {StyleSheet} from 'react-native';

const styles = (screenContext, width, height) =>
  StyleSheet.create({
    drawerItemStyle: {
      borderTopWidth: 1,
      borderTopColor: 'grey',
    },
  });
export default styles;
